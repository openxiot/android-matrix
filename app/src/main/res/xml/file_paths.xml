<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      只放行下载安装包的那一个目录。
      不要图省事写成 path="."：那等于把整个 filesDir 交给任何拿到一次 URI 授权的组件，
      而拿到授权的是系统安装器 —— 它只需要读那一个 APK。
    -->
    <files-path
        name="updates"
        path="updates/" />
</paths>
